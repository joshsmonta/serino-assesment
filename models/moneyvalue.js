export default class MoneyValue {
    constructor(id, treasureId, amt) {
        this.id = id;
        this.treasureId = treasureId;
        this.amt = amt;
    }

    // async addAmount(treasure_id, amt) {}
}