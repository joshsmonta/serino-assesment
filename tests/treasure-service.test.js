import { describe, beforeEach, test, expect } from "vitest";
// import { grabTreasureService } from '../services/user-service.js';

describe('Treasure Service', () => {
    test.todo('it should be able to successfully grab a treasure using user_id and treasure_id')

    test.todo('it should fail when user_id or treasure_id is not found')

    test.todo('it should fail when user_id already claimed this treasure')
})